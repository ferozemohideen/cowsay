---
apps:
  my-app-web:
    run: "flask --app web run --host 0.0.0.0 --port $PORT"
    config:
      ingress:
        enabled: true
        custom_domain: true
        hosts: ["test-python.feroze.porter.run"]

  my-app-wkr:
    run: "python worker.py"
